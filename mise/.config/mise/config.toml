[tools]
1password = "latest"
age = "latest"
"aqua:aws/session-manager-plugin" = "latest"
ast-grep = "latest"
aws-cli = { version = "latest", symlink_bins = "true" }
carapace = "latest"
"cargo:abtop" = "latest"
"cargo:anglosaxon" = "latest"
"cargo:https://github.com/nushell/nufmt" = "latest"
fnox = "latest"
fzf = "latest"
github-cli = "latest"
aws-vault = "latest"
television = "latest"
"github:aws-cloudformation/rain" = "latest"
gum = "latest"
"github:control-theory/gonzo" = "latest"
"github:docker/compose" = "latest"
"github:hedhyw/json-log-viewer" = "latest"
nu = "latest"
"github:pb33f/openapi-changes" = "latest"
starship = "latest"
"github:walles/moor" = "latest"
go = "latest"
"go:github.com/isaacphi/mcp-language-server" = "latest"
jaq = "latest"
java = "zulu-25"
jwt = "latest"
maven = "3"
node = "24"
"npm:@mermaid-js/mermaid-cli" = "latest"
"npm:@pb33f/openapi-changes" = "latest"
"npm:exa-mcp-server" = "latest"
"npm:defuddle" = "latest"
"npm:typescript-language-server" = "latest"
"pipx:awslogs" = "latest"
"pipx:posting" = "latest"
# postgres-mcp 0.3.0 declares mcp[cli]>=1.5.0; mcp 2.0 removed mcp.server.fastmcp,
# so the dependency must stay on the 1.x line.
"pipx:postgres-mcp" = { version = "latest", uvx_args = "--with mcp[cli]<2" }
"pipx:sqlit-tui" = "latest"
"pipx:xmlformatter" = "latest"
python = "latest"
rust = "latest"
sops = "latest"
viddy = "latest"
docker-cli = "latest"
gitu = "latest"
lazydocker = "latest"
lazygit = "latest"
neovim = "latest"
"github:docker/buildx" = "latest"
superfile = "latest"
uv = "latest"
"pipx:mcp2cli" = "latest"
"npm:@earendil-works/pi-coding-agent" = "latest"
mitmproxy = "latest"
"github:mfontanini/presenterm" = "latest"
"github:riii111/sabiql" = "latest"
"github:bensadeh/tailspin" = "latest"
"npm:dotenv-cli" = "latest"
"github:always-further/nono" = "latest"
"github:BurntSushi/bttf" = "latest"
"github:achristmascarl/rainfrog" = "latest"
caddy = "latest"
zellij = "latest"
"github:pb33f/wiretap" = "latest"
watchexec = "latest"
"cargo:rgrc" = { version = "latest", features = "embed-configs" }
# Pinned, and on github: rather than aqua:. The aqua registry entry carries
# `rosetta2: true`, and mise's actual_arch() rewrites arm64 to amd64 whenever
# that is set, without checking whether an arm64 asset exists — so aqua hands
# this machine the x86_64 build even though ouch ships aarch64-apple-darwin.
# 0.8.2 is the last version whose attestations mise's github: backend can
# resolve; 0.8.3 is a hotfix cut from a branch and trips a false "no provenance
# verification" error that fails `mise lock`. Its only change is a `--version`
# fix for source builds, which does not apply to a downloaded binary.
"github:ouch-org/ouch" = "0.8.2"
"github:pb33f/printing-press" = "latest"
sqlite = "latest"
"github:pamburus/hl" = "latest"
glow = "latest"
"cargo:mdcat" = "latest"
herdr = "latest"
"github:agavra/tuicr" = "latest"
hunk = "latest"
bun = "latest"
"github:huseyinbabal/taws" = "latest"
"github:fioncat/otree" = "latest"
actionlint = "latest"
"github:dmtrKovalenko/fff" = { version = "latest", matching = "fff-mcp", exe = "fff-mcp" }
pre-commit = "latest"
"aqua:github/github-mcp-server" = "latest"
"aqua:anthropics/claude-code" = "latest"
hwatch = "latest"
"ubi:thiagokokada/gh-gfm-preview" = "latest"
usage = "6.9.0"

[settings]
# No `lockfile = true`: that creates a mise.lock in every project mise touches,
# and only this repo's tools are worth locking. Unset still *updates* lockfiles
# that already exist, and the global one is written by `mise lock --global` in
# the update:mise task either way — see mise.toml.
experimental = true
idiomatic_version_file_enable_tools = []
env_cache = true
use_versions_host = true

# The 24h default (mise's own) is the supply-chain guard for the ~75 tools
# above: a yanked or broken "latest" gets caught before it lands. These are the
# exceptions — agent tooling and small single-author CLIs where a same-day fix
# is the reason to upgrade at all, and where waiting a day is the cost rather
# than the protection. Full backend IDs, because a shorthand only matches if
# the tool is declared by shorthand.
minimum_release_age_excludes = [
  "herdr",
  "hunk",
  "github:agavra/tuicr",
  "github:dmtrKovalenko/fff",
  "github:always-further/nono",
  "aqua:github/github-mcp-server",
  "aqua:anthropics/claude-code",
  "pipx:mcp2cli",
  "go:github.com/isaacphi/mcp-language-server",
  "npm:@earendil-works/pi-coding-agent",
  "npm:exa-mcp-server",
  "github:BurntSushi/bttf",
  "github:huseyinbabal/taws",
  "github:riii111/sabiql",
  "github:fioncat/otree",
  "github:achristmascarl/rainfrog",
  "github:pb33f/printing-press",
  "github:pb33f/wiretap",
  "github:control-theory/gonzo",
  "github:walles/moor",
  "github:pamburus/hl",
  "github:bensadeh/tailspin",
  "github:mfontanini/presenterm",
]

[settings.github]
credential_command = "fnox get MISE_GITHUB_TOKEN"

[env]
# Commands that must run under `fnox exec` to receive their credentials. mise
# orders these ahead of tool directories, so the wrapper here wins over the
# maven that mise itself provisions.
_.path = ["~/.local/bin/wrapped"]
